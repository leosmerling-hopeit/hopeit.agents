name: MCP Plugins Test PyPI Publishing

on:
  release:
    types: [unpublished, prereleased]

jobs:
  upload:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: latest

      - name: Install development dependencies
        run: make install-dev

      - name: Lint
        run: make lint

      - name: Test
        run: make test

      - name: Build MCP server plugin
        run: make dist-plugin PLUGINFOLDER=plugins/mcp/mcp-server

      - name: Build MCP client plugin
        run: make dist-plugin PLUGINFOLDER=plugins/mcp/mcp-client

      - name: Publish MCP server plugin to Test PyPI
        env:
          TEST_PYPI_API_TOKEN: ${{ secrets.TEST_PYPI_API_TOKEN }}
        run: make publish-plugin-pypi-test PLUGINFOLDER=plugins/mcp/mcp-server

      - name: Publish MCP client plugin to Test PyPI
        env:
          TEST_PYPI_API_TOKEN: ${{ secrets.TEST_PYPI_API_TOKEN }}
        run: make publish-plugin-pypi-test PLUGINFOLDER=plugins/mcp/mcp-client
